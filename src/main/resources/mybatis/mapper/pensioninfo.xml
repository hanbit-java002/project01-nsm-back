<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pension">

  <select id="pensionInfo" resultType="map">
    SELECT title, description, photo, homepage, 
           reservation, addr, new_addr, tel, chk_in, chk_out
      FROM tbl_pension_info
  </select>

</mapper>